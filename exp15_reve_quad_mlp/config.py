"""Configuration for Experiment 15: REVE-based quad-modal fusion.

Mirrors exp7_all_modalities.config but with REVE-specific paths and a
512-dim per-window EEG feature dimension. Training hyperparameters
(MLP_CONFIG, CV_CONFIG) are identical to exp7 so the apples-to-apples
comparison against the published Exp7a-EEG2Vec headline is direct.
"""

from pathlib import Path

# Paths
BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR.parent / "asm_data"
CSV_PATH = DATA_DIR / "alfred_1st_regimen.csv"
OUTPUTS_DIR = BASE_DIR / "outputs"
RESULTS_DIR = OUTPUTS_DIR / "exp15_results"

# Pre-computed REVE-base per-window features
# Shape (n_patients, max_windows=120, embed_dim=512), pids array,
# valid_window_counts array
REVE_FEATURES_PATH = OUTPUTS_DIR / "reve_features_alfred.npz"

# Clinical feature configuration (re-used from exp4)
CLINICAL_CONFIG = {
    "numeric_features": ["age_init"],
    "binary_features": [
        "sex", "pretrt_sz_5", "focal", "fam_hx", "febrile",
        "ci", "birth_t", "head", "drug", "alcohol",
        "cvd", "psy", "ld",
    ],
    "categorical_features": ["lesion", "eeg_cat"],
    "input_dim": 19,
}

# Text embeddings (re-used from exp1/exp3)
TEXT_EMBEDDINGS = {
    "clinicalbert": OUTPUTS_DIR / "bert_alfred_1stregimen_eeg_embeddings.npy",
    "pubmedbert": OUTPUTS_DIR / "pubmedBert_alfred_1stregimen_eeg_embeddings.npy",
}

# SMILES embeddings (re-used from exp1/exp3)
SMILES_EMBEDDINGS = {
    "chemberta": OUTPUTS_DIR / "chemberta_asm_embeddings.npy",
}

ASM_NAMES_FILE = OUTPUTS_DIR / "asm_drug_names.txt"

# Embedding dimensions
CLINICAL_DIM = 19
TEXT_DIM = 768
REVE_DIM = 512        # REVE-base attention-pooled per-window embedding
EEG_AGG_DIM = 256     # the aggregator's embed_dim - matches exp7's EEG2Vec aggregator
SMILES_DIM = 768

# EEG / REVE windowing parameters (must match the cache shape; the cache
# was generated by analysis/reve_extract_features.py over the
# std-19 EEG cache which uses these settings).
EEG_CONFIG = {
    "target_sr": 200,
    "min_duration_sec": 600,
    "skip_start_sec": 300,
    "use_duration_sec": 1200,
    "window_sec": 10,
}
MAX_WINDOWS = int(EEG_CONFIG["use_duration_sec"] / EEG_CONFIG["window_sec"])  # 120

# Aggregator config (matches exp7 EEGWindowTransformer)
AGGREGATOR_CONFIG = {
    "embed_dim": EEG_AGG_DIM,
    "num_heads": 4,
    "num_layers": 2,
    "max_windows": MAX_WINDOWS,
}

# MLP training configuration (identical to exp7)
MLP_CONFIG = {
    "batch_size": 8,
    "learning_rate": 1e-3,
    "weight_decay": 1e-4,
    "epochs": 100,
    "patience": 20,
    "dropout": 0.3,
    "hidden_dim": 64,
    "num_classes": 2,
}

# Cross-validation config (identical to exp7)
CV_CONFIG = {
    "n_splits": 5,
    "shuffle": True,
    "random_state": 42,
}

# ASM name mapping (re-used from exp7)
ASM_NAME_MAPPING = {
    "LEV": "Levetiracetam", "VPA": "Valproic_acid", "LTG": "Lamotrigine",
    "CBZ": "Carbamazepine", "cBZ": "Carbamazepine", "PTN": "Phenytoin",
    "TPM": "Topiramate", "OXC": "Oxcarbazepine", "LCM": "Lacosamide",
    "BRV": "Brivaracetam", "PER": "Perampanel", "ZNS": "Zonisamide",
    "GBP": "Gabapentin", "PGB": "Pregabalin", "CLB": "Clobazam",
    "CZP": "Clonazepam",
}

# Outcome mapping: 1=failure->0, 2=success->1 (identical to exp7)
OUTCOME_MAPPING = {1: 0, 2: 1}

# Experiment definitions - only Exp7a-equivalent (MLP + ClinicalBERT + ChemBERTa)
EXPERIMENTS = [
    {
        "name": "exp15_reve_clinicalbert_chemberta",
        "fusion": "mlp",
        "text_model": "clinicalbert",
        "smiles_model": "chemberta",
    },
]
